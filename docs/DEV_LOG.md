# Skye ERP - Development Log

Internal progress/context log for continuing this build across sessions. Not user-facing (see `USER_GUIDE.md` for that). Update this after every phase or significant milestone.

## Project identity

- Repo: https://github.com/skyeautomotives/ERP (branch `main`)
- Local path: `D:\Skye ERP`
- Supabase project: ref `yvvquzofuqkhwbmbqhcs`, region ap-south-1 (Mumbai), org "skyeautomotives's Org"
- Supabase project URL: `https://yvvquzofuqkhwbmbqhcs.supabase.co`
- Stack: Next.js (App Router, Turbopack) + TypeScript + Tailwind v4 + Supabase (Postgres/Auth/Storage), installable PWA. Single codebase for web + mobile-responsive use, no separate native app.
- Admin login: `vjoelvarghese@gmail.com` (the user, Joel Varghese)
- Master spec: a 60-section requirements doc the user pasted at project start (referenced throughout as "the spec" / "section N"). Phase order for the build is spec section 57.

## Phase status (spec section 57 order)

- [x] **Phase 1** - Auth, RBAC, company setup, PWA shell. Shipped 2026-08-17.
- [x] **Phase 2** - Customer/Supplier/Product/Route masters. Shipped 2026-08-17.
- [x] **Phase 3** - Sales (credit, cash, sales orders). Shipped 2026-08-17.
- [x] **Dark mode** - real `dark:` Tailwind support added across the whole app (not a spec phase, but a full pass). Shipped 2026-08-17.
- [x] **Phase 4** - Purchase + Purchase Verification. Shipped and verified end-to-end 2026-08-17.
- [x] **Phase 5** - Inventory. Shipped and verified end-to-end 2026-08-17. **A cleanup-script mistake during this phase accidentally deleted one of the user's own real sales invoices (INV00001) - see Gotcha #9. Flagged to the user directly; not silently noted.**
- [x] **Phase 6** - Cash/Bank Receipts + Cash/Bank Payments + Expenses. Shipped and verified end-to-end 2026-08-17.
- [x] **Phase 7** - Accounting + Ledgers + Trial Balance + P&L + Balance Sheet. Shipped and verified end-to-end 2026-08-17.
- [x] **Phase 8** - GST Module + GSTR Reports. Shipped and verified end-to-end 2026-08-17.
- [x] **Phase 9** - Staff + Sales Performance + Collection Performance. Shipped and verified end-to-end 2026-08-17.
- [x] **Phase 10** - Incentive Engine. Shipped and verified end-to-end 2026-08-17.
- [x] **Phase 11** - Dashboard + Analytics. Shipped and verified end-to-end 2026-08-18.
- [x] **Phase 12** - Mobile Application. Shipped and verified end-to-end 2026-08-18.
- [x] **Phase 13** - Realtime Sync + Offline Queue. Shipped and verified end-to-end 2026-08-18.
- [ ] **Phase 14** - Security Audit + Testing + Performance Optimization. **NEXT UP.**
- [ ] Phase 14 - Security Audit + Testing + Performance Optimization

User said (2026-08-17): "continue with the rest of the phases" - blanket approval to keep going through all remaining phases without stopping to ask between each. Still using EnterPlanMode per phase (has caught real design decisions every time), just not pausing for a go-ahead between phases.

## Phase 4 (Purchase + Purchase Verification) - COMPLETE

Plan file used: `C:\Users\vjoel\.claude\plans\federated-popping-engelbart.md` (gets overwritten each phase - this log is the durable record, that file is scratch/current-phase-only).

**What shipped:**
- Migrations: `20260817300001_purchase_prep.sql` (company_settings purchase numbering cols + products.last_purchase_rate/last_purchase_date), `20260817300002_purchase_invoices.sql`, `20260817300003_purchase_verifications.sql`, `20260817300004_purchase_rpcs.sql` (create_purchase_invoice, cancel_purchase_invoice, record_purchase_verification).
- Nav: "purchase" module live (Purchase Entry, Bill Verification real links; Purchase Return/Reports coming-soon).
- Settings > Company: added `purchase_ref_prefix` field alongside the existing sales `invoice_prefix`.
- Refactored two components for cross-module reuse (sales + purchase both need them), moved to `src/components/`: `LineItemRow` (renamed its rate-autofill field from `selling_rate` to generic `default_rate` - callers now alias `default_rate:selling_rate` or `default_rate:purchase_rate` in their `.select()`), and `CancelInvoiceButton` (now takes an `action` prop instead of being hardcoded to sales).
- Built: `purchase/page.tsx` (redirect), `purchase/entries/{actions.ts, purchase-invoice-form.tsx, page.tsx, new/page.tsx, [id]/page.tsx, verification-panel.tsx}`, `purchase/verification/page.tsx` (status queue, filterable).
- Full Playwright verification pass: intra-state purchase (CGST/SGST) correct, inter-state (IGST) correct, duplicate supplier-invoice-number blocked then override flow worked and was recorded, all three verification statuses (matched/partial/mismatch) computed correctly by the RPC (confirmed directly against the DB, not just the UI - one screenshot caught the UI mid-save and looked like a false negative, DB was actually correct), stock increased on purchase and correctly reversed on cancel, `products.last_purchase_rate` updated.
- Test data cleaned up afterward. **Found unrelated pre-existing data during cleanup** - a product "123"/"Test" and one sales invoice INV00001 that none of my scripts created, most likely the user's own manual exploration of the dev server in their browser - deliberately left alone rather than deleted (see Gotchas #8, new).
- `docs/USER_GUIDE.md` Phase 4 section added.

**Fast-follow / not done (intentionally, matches spec's Phase 4 charter exactly):** Purchase Returns, Purchase Reports - shown as "Coming soon", same as every other unbuilt screen.

## Phase 5 (Inventory) - COMPLETE, but read this before touching cleanup scripts again

**What shipped:**
- Migrations: `20260817400001_stock_view.sql` (`product_stock_levels`, a `security_invoker` view - current qty/value per product), `20260817400002_stock_adjustments.sql` (`stock_adjustments` table + `create_stock_adjustment` RPC), `20260817400003_stock_as_of.sql` (`get_stock_as_of(date)` RPC - same shape as the view, date-bounded, powers the "opening/closing stock" picker), `20260817400004_movement_analysis.sql` (`get_movement_analysis(days)` RPC - fast/slow moving ranking).
- Nav: "inventory" module live (Stock Report, Stock Adjustment, Movement Analysis - nothing marked coming-soon, section 19's whole list is covered per the plan's "fold into existing pages" design).
- Built: `inventory/page.tsx` (redirect), `inventory/stock/page.tsx` (main report: search, low-stock filter, as-of date, total value card), `inventory/stock/[productId]/page.tsx` (movement ledger with running balance), `inventory/adjustments/{actions.ts, adjustment-form.tsx, page.tsx}`, `inventory/analysis/page.tsx`.
- Caught and fixed the same Server-Component-can't-take-onChange bug as Phase 2 (the "as of date" picker) before it shipped - used a plain submit button instead of auto-submit-on-change.
- Full Playwright + direct-DB verification: opening/purchase/sale/adjustment math all correct (58 = 50 opening + 10 purchase - 5 sale + 3 adjustment, value 2900 = 58 x unit_cost 50), low-stock filter correctly included/excluded the right products, as-of-yesterday correctly excluded same-day transactions, movement analysis correctly ranked the sold product as fast-moving.

**Incident during this phase's cleanup - read Gotcha #9 below.** The cleanup script used unfiltered `sales_invoices?id=not.is.null` / `sales_invoice_items?id=not.is.null` deletes (correct in Phase 3/4 because those tables were verified empty beforehand) and this time swept up the user's own pre-existing sales invoice (INV00001, for their "Test"/123 product) along with the test rows. The product itself was not deleted, only the invoice and its stock transaction. **This was reported to the user directly in the same turn it was discovered**, not silently absorbed - if you're resuming this project and don't see that conversation, ask the user whether they still need that invoice recreated (I have no way to reconstruct its exact original contents).

**Fast-follow / not done:** "Stock Transfer" (section 19) skipped entirely - no warehouse/location concept exists anywhere in the spec's master data or what's been built; would require inventing a new master not requested elsewhere.

## In-app contextual help - COMPLETE

User asked (2026-08-17) for a detailed field-by-field user guide plus in-app "?" help on every screen, same cross-cutting treatment as dark mode. Plan file used: `C:\Users\vjoel\.claude\plans\federated-popping-engelbart.md` (will be overwritten by the next phase's plan - this section is the durable record).

**Design (approved):** one reusable `HelpButton` ("?" icon next to each page's `<h1>`, opens a popover) fed by centralized content in `src/lib/help-content.ts`, rather than per-field inline tooltips everywhere. Content keyed by feature slug, reused across a feature's list/new/detail pages.

**Done:**
- `src/components/help-button.tsx` - the reusable component (click-outside + Escape to close, full dark: variants). Builds clean, not yet imported anywhere.
- `src/lib/help-content.ts` - all 15 content entries written (company-settings, users, roles, customers, suppliers, products, routes, credit-sales, cash-sales, sales-orders, sales-invoice-detail, purchase-entries, purchase-verification, stock-report, stock-adjustments, movement-analysis).

**Wired in** to all ~20 pages: settings/{company,users,roles}, masters/{customers,suppliers,products,routes} (list + new), sales/{credit,cash,orders} (list + new) + sales/[id] + sales/orders/[id], purchase/entries (list + new + [id]) + purchase/verification, inventory/{stock,adjustments,analysis}. Pattern used everywhere: wrap the page's `<h1>` in `<div className="flex items-center gap-2">` alongside `<HelpButton content={HELP_CONTENT["slug"]} />`.

Verified with Playwright in dark color scheme across 5 representative screens (Company Setup, New Customer, Credit Sales, Purchase Verification, Stock Report): panel opens with real content, closes on Escape, renders cleanly. Screenshot-checked visually too - clean and readable.

**Standing convention now**: every future phase's new pages get a `HELP_CONTENT` entry in `src/lib/help-content.ts` + `<HelpButton>` next to the `<h1>` from the start, same as `dark:` variants and `autoComplete="off"` already are.

## Phase 6 (Cash/Bank Receipts + Payments + Expenses) - COMPLETE

**What shipped:**
- Migrations: `20260817500001_accounts_prep.sql` (receipt/payment numbering columns on company_settings + `expense_categories` master), `20260817500002_receipts_payments.sql` (`receipts`/`receipt_allocations`, `payments`/`payment_allocations` - cash/bank unified via a `method` column, same simplification as `sales_invoices.sale_type`), `20260817500003_outstanding_views.sql` (`sales_invoice_outstanding`/`purchase_invoice_outstanding` - the data Phase 7's ledgers will read from), `20260817500004_receipt_rpcs.sql`, `20260817500005_payment_rpcs.sql` (create_/cancel_ for both, same atomic-numbering + validation pattern as create_sales_invoice).
- Key design point: **no separate `expenses` table** - an expense is just a `payments` row with `purpose='expense'` + `expense_category_id`; `/accounts/expenses` is a filtered report over `payments`, not its own data source.
- Nav: "accounts" module live (Cash/Bank Receipt, Cash/Bank Payment, Expenses real; Customer Ledger/Supplier Ledger/Journal coming-soon for Phase 7). "Expense Categories" added under Masters.
- Built: `masters/expense-categories/{actions.ts,category-form.tsx,page.tsx}` (Users-page-style inline-form pattern, no separate detail page needed for a one-field master), `accounts/receipts/{actions.ts,receipt-form.tsx,cash/,bank/,[id]/page.tsx}`, `accounts/payments/{actions.ts,payment-form.tsx,cash/,bank/,[id]/page.tsx}`, `accounts/expenses/page.tsx`, `accounts/page.tsx` (redirect).
- `ReceiptForm`/`PaymentForm` are the biggest new UI pieces: bill-mode allocation is a live table of the counterparty's outstanding invoices (queried client-side from the outstanding views as soon as a customer/supplier is picked), with per-invoice amount inputs that must sum exactly to the receipt/payment total - enforced client-side for UX and server-side in the RPC for real (the RPC is the actual authority, never trust the client).
- Full Playwright + direct-DB verification: intra-flow math confirmed correct via the outstanding views (sales invoice outstanding went from 1180 -> 0 after a full bill-mode receipt, purchase invoice outstanding 1180 -> 0 after a full bill-mode payment), on-account receipt/payment recorded without needing an invoice, expense payment showed up correctly on the Expenses report with category totals, cancelling a receipt correctly put the invoice back to fully outstanding (the outstanding views only sum `status='active'` allocations, so cancellation "just works" without extra logic).
- **One verification gap, noted honestly rather than silently skipped**: didn't get an independent UI-driven confirmation that the RPC's "allocation exceeds outstanding balance" server-side guard actually fires - the input's HTML `max` attribute pre-empted the browser's form submission before it could be tested that way. The guard is in the RPC (`if v_alloc_amount > v_invoice.outstanding_amount + 0.01 then raise exception`) and was code-reviewed, but not independently exercised. Worth a real test if this becomes load-bearing later.
- Test data cleanup this time **followed the Phase 5 lesson properly**: queried every transactional table's exact contents first, found a real pre-existing customer ("Vishnumaya Automobiles") the user had created that a blind delete would have caught, and deleted only the specific IDs created during this test run (confirmed via per-call response counts, not assumed).

**Fast-follow / not done:** Customer Ledger, Supplier Ledger, Journal - explicitly Phase 7 per the spec's own phase list. Re-allocating an old on-account receipt/payment to a later invoice isn't built (recording on-account is in scope, applying it afterward is not).

## Phase 7 (Accounting + Ledgers + Trial Balance + P&L + Balance Sheet) - COMPLETE

**What shipped:**
- Migrations `20260817600001` - `20260817600006`: `chart_of_accounts` (12 seeded accounts: 1000 Cash, 1010 Bank, 1100 Accounts Receivable, 1200 Inventory, 1300 GST Input Credit, 2000 Accounts Payable, 2100 GST Payable, 3000 Opening Balance Equity, 4000 Sales Income, 5000 Cost of Goods Sold, 5100 Operating Expenses, 5200 Inventory Adjustments), `journal_entries`/`journal_entry_lines` (locked tighter than any table so far - no insert/update policy for `authenticated` at all, only `security definer` triggers write), `journal_triggers.sql` (posting trigger functions on sales_invoices/purchase_invoices/receipts/payments/stock_adjustments/customers/suppliers/products), `backfill_journal.sql` (one-time backfill for pre-existing data), `reporting_functions.sql` (`get_account_balances`, `get_customer_ledger`, `get_supplier_ledger`), and `fix_invoice_journal_triggers.sql` (bugfix, see Gotcha #10 below).
- **New architecture: automatic double-entry posting via triggers, not RPC edits.** Every sale, purchase, receipt, payment, and stock adjustment now silently posts a balanced journal entry the moment it's saved - no existing RPC bodies were touched. See the new "Journal posting" convention below; **every future phase that adds a new money-moving table must add a matching trigger**, or that table's activity will be invisible to Trial Balance/P&L/Balance Sheet.
- Nav: Customer Ledger/Supplier Ledger/Journal flipped from "Coming soon" to real; added Bill-wise Outstanding, Trial Balance, Profit & Loss, Balance Sheet under Accounts, and Chart of Accounts under Masters (view-only - no custom-account creation yet, since a custom account can't receive postings without a manual journal entry screen, a documented fast-follow).
- Built: `masters/chart-of-accounts/page.tsx`, `accounts/journal/page.tsx` (doubles as Cash Book/Bank Book/Day Book via account/date filter presets - one page, not three), `accounts/customer-ledger/page.tsx` + `supplier-ledger/page.tsx` (running balance via `get_customer_ledger`/`get_supplier_ledger`, labeled "Billed/Received" and "Billed/Paid" rather than raw Dr/Cr jargon), `accounts/outstanding/page.tsx` (tabbed, ageing buckets 0-15/16-30/31-45/46-60/61-90/90+ days - purchase invoices don't store a due_date, so it's computed as `supplier_invoice_date + credit_period_days` in the query), `accounts/trial-balance/page.tsx`, `accounts/profit-and-loss/page.tsx`, `accounts/balance-sheet/page.tsx` (Current Earnings computed as all-time income minus all-time expenses up to the as-of date, since there's no periodic closing process).
- **Caught and fixed during this phase's own verification** (not left for later): `create_sales_invoice`/`create_purchase_invoice` insert the header row first with zero totals, then a separate UPDATE (status unchanged) fills in the real totals once line items are processed. The original triggers posted on INSERT and never re-fired for that finalizing UPDATE, so sales/purchase journal entries landed with correct accounts but **zero amounts** - caught by cross-checking journal_entry_lines directly against the DB (not just trusting the UI's "Balanced" message, which was technically true since 0=0). Fixed in `20260817600006_fix_invoice_journal_triggers.sql` - see Gotcha #10.
- Full Playwright + direct-DB verification: a credit sale posted Dr AR 1180 / Cr Sales Income 1000 / Cr GST Payable 180; a purchase posted Dr Inventory 500 / Dr GST Input Credit 90 / Cr Accounts Payable 590; a cash receipt and bank payment posted correctly against Cash/Bank; Trial Balance totals matched (3540 = 3540) and displayed "Balanced"; Balance Sheet displayed "Balanced" with Assets = Liabilities + Equity; Customer Ledger showed the invoice and receipt with a running balance ending at 0.00; Bill-wise Outstanding correctly excluded the now-fully-paid invoice.
- **Verification gap, noted honestly rather than silently skipped**: live-cancelling a sales invoice through the app (to independently exercise the reversing-entry trigger branch) was blocked mid-session by the environment's own auto-mode safety classifier, both as a direct RPC call and as a Playwright-driven UI click - it read as an unreviewed mutating action against what looks like a production database. Did not attempt to work around it. The cancel/reversal logic is a direct structural mirror of the already-verified posting logic (same account pairs, debit/credit swapped) and follows the identical pattern already proven correct for receipts/payments in Phase 6 - reviewed but not independently live-tested this pass.
- Test data cleanup followed the Phase 5/6 lesson: queried every table's exact contents first (confirmed "Vishnumaya Automobiles" and the "123"/"Test" product predate this phase and left them alone), deleted only the specific IDs this phase's own scripts created, in FK-dependency order, confirming a clean final state afterward.

**Fast-follow / not done:** Manual journal entry screen (for accountant-driven adjustments not tied to any transaction) - Chart of Accounts is view-only this phase for exactly this reason. Re-allocating an on-account receipt/payment to a later invoice (carried over from Phase 6).

## Phase 8 (GST Module + GSTR Reports) - COMPLETE

**Confirmed against the actual spec text** (extracted from the original session transcript since the spec wasn't saved as a file - sections 36-37): GST Module needs B2B/B2C distinction, credit notes, debit notes, HSN summary (sales side) and supplier GSTIN/invoice details/input tax credit (purchase side). GSTR Reports needs GSTR-1 data, GSTR-2/purchase data, GST summary, sales/purchase GST reports, input tax credit report, output GST report, GST reconciliation, export capability, and an explicit Draft/Verified/Ready for filing/Filed status distinction - **with an explicit instruction not to claim the system itself has filed anything** unless a real filing API integration exists (it doesn't, and none was built).

**What shipped:**
- Migrations `20260817700001` - `20260817700007`: `returns_prep.sql` (credit/debit note numbering columns), `credit_notes.sql`/`debit_notes.sql` (+ `_items` tables, same RPC-only lockdown as every transactional table), `return_rpcs.sql` (`create_credit_note`/`cancel_credit_note`, `create_debit_note`/`cancel_debit_note` - validate returned quantity against what's actually left on the original invoice line across all prior active notes), `return_journal_triggers.sql` (`post_credit_note_journal`/`post_debit_note_journal`), `gst_return_periods.sql` (plain has_permission-gated table, not RPC-locked - it's a workflow tracker, not money), `gst_reporting_functions.sql` (`get_gstr1_b2b`, `get_gstr1_b2c_summary`, `get_hsn_summary`, `get_purchase_register`).
- **Key design decision: Credit Notes and Debit Notes ARE Sales Return and Purchase Return.** Both had sat as "Coming soon" at `/sales/returns`/`/purchase/returns` since Phase 3/4 - a GST credit note is the real-world compliance document for a sales return, so building it now retires both placeholders and satisfies section 36 in the same move. Full item-level return flow: pick an original invoice, pick which product lines and how much of each to return, reason required, stock moves back the opposite direction of the original transaction, GST/revenue reverses proportionally.
- **Applied Phase 7's Gotcha #10 correctly from the start this time**: `create_credit_note`/`create_debit_note` follow the same insert-header-then-finalize-totals pattern as every other invoice RPC (line items need the parent row's id before totals are known), so their journal triggers were written from day one to post on `OLD.total_amount = 0 AND NEW.total_amount <> 0`, not on INSERT. Verified directly against `journal_entry_lines` (not just a UI message) - a 3-of-10-unit credit note posted Dr Sales Income 300 / Dr GST Payable 54 / Cr Accounts Receivable 354 correctly on the first try, no repair migration needed this time.
- Nav: "gst" module now built (GSTR-1, HSN Summary, Purchase Register, GST Reconciliation, GST Returns); `/sales/returns` and `/purchase/returns` flipped off `comingSoon`.
- Built: `sales/returns/{actions.ts, credit-note-form.tsx, page.tsx, new/page.tsx, [id]/page.tsx}`, `purchase/returns/{...}` (identical shape, mirrored for purchases/suppliers), `gst/gstr1/page.tsx` (B2B invoice-wise + B2C by-rate summary, period filter), `gst/hsn-summary/page.tsx` (sales/purchase tab, grouped by HSN + rate), `gst/purchase-register/page.tsx`, `gst/reconciliation/page.tsx` (GSTR-1 output tax minus this period's credit notes vs. the GST Payable account's net period activity from Phase 7's `get_account_balances`; same idea for input tax vs GST Input Credit), `gst/returns/{actions.ts, page.tsx, new-period-form.tsx, gst-return-row.tsx}` (Draft -> Verified -> Ready for filing -> Filed checklist, with UI copy explicit that nothing is actually filed by the app), `src/components/export-csv-button.tsx` (client-side Blob download, reused across every GST report - no server export endpoint needed).
- **Scope decision, not a silent gap**: credit/debit notes correctly reduce the GST Payable/Input Credit ledger accounts and get their own list/report, but aren't merged into GSTR-1's own "9B" credit/debit note table - the Reconciliation page explicitly nets them out when comparing GSTR-1 to the ledger so this doesn't look like a false mismatch.
- **B2B vs B2C**: determined by whether `customers.gstin` is set, not by sale_type - so a "credit sale" to a customer without a GSTIN still lands in B2C. A credit note can only be raised against an invoice with a real `customer_id` (not a walk-in cash sale) - enforced by only offering such invoices in the picker, not by a new DB constraint.
- Full Playwright + direct-DB verification: credit sale + purchase created, a 3-of-10 credit note and a 4-of-10 debit note created and their journal entries confirmed correct and non-zero directly against the database; GSTR-1 B2B correctly listed the invoice with its GSTIN; HSN Summary correctly grouped by the test product's HSN code; Purchase Register showed the supplier; GST Reconciliation showed "Matched" on both checks; Chart of Accounts showed Sales Income net of the credit note (700 = 1000 - 300); GST Returns tracker created a period and advanced Draft -> Verified.
- **One assertion in the test script, not the app, was wrong**: tried to independently verify the RPC's returned-quantity-exceeded guard via a UI value above the remaining amount, but the input's HTML `max` attribute pre-empts the browser before it can submit - identical, already-documented gap from Phase 6's allocation guard. The RPC-side check was code-reviewed (mirrors the exact same over-allocation guard pattern proven correct in `create_receipt`).
- Test data cleanup followed the established Gotcha #9 process: queried every table first, confirmed "Vishnumaya Automobiles" and the "123"/"Test" product predate this phase, deleted only this run's specific IDs in FK order, confirmed a clean final state.

**Fast-follow / not done:** No literal GST-portal JSON upload format (export is CSV, per the spec's own instruction not to claim real filing integration). Credit/debit notes not yet merged into a combined GSTR-1 "9B" table (tracked separately, correctly reflected in the ledger).

## Phase 9 (Staff + Sales Performance + Collection Performance) - COMPLETE

**Confirmed against the actual spec text** (sections 28-31, same transcript-extraction approach as Phase 8): Credit Collection Tracking (28), the "Credit Collection Within 90 Days" report (29), Sales by Staff (30), Route-wise Sales (31). Sections 32-34 (Incentive System, slabs, dashboard) are confirmed Phase 10, not this one.

**What shipped:**
- One migration, `20260817800001_staff_performance_functions.sql` - pure reporting, no new tables: `get_staff_performance(from,to)` (one row per staff - total/credit sales, invoice/customer counts, collection, outstanding, returns, profit, plus six collection-ageing columns), `get_collection_detail(from,to)` (one row per receipt allocation against a credit sale - the 90-day collection report), `get_route_performance(from,to)` and `get_route_staff_breakdown(from,to)` (route-wise sales/collection/outstanding plus a route+staff cross-tab).
- Nav: new "staff" module (Staff Performance, Collection Report, Route Performance), reusing the `"staff"` RBAC module key that had been seeded since Phase 1 but never built against.
- Built: `staff/performance/page.tsx`, `staff/collection-report/page.tsx`, `staff/route-performance/page.tsx` - all three share the Today/This Week/This Month quick-preset + custom-range pattern from Phase 7's Journal page. Profit columns gated by the same `PROFIT_VISIBLE_ROLES` array already used on the sales invoice detail page.
- **Two real, pre-existing bugs found and fixed while building this phase's own verification** (not deferred):
  1. **Cash sales were silently sitting as permanent "outstanding" balances.** `sales_invoice_outstanding` (Phase 6) and `get_customer_ledger` (Phase 7) computed billed/outstanding off every `sales_invoices` row regardless of `sale_type`, and `post_sales_invoice_journal` (Phase 7) always debited Accounts Receivable - even for a cash sale, which is paid in full at the counter and never goes through the receipts flow. Surfaced when a staff member's "total outstanding" in this phase's new report included their cash sales. Fixed in `20260817800002_fix_cash_sale_outstanding.sql`: the view now only considers `sale_type = 'credit'`, `get_customer_ledger` does the same, and the journal trigger now debits Cash (account `1000`) instead of Accounts Receivable when `sale_type = 'cash'`. No historical cash-sale data existed in the database at the time, so no backfill/repair was needed - only the three function definitions.
  2. **PostgREST cannot auto-detect an embeddable relationship from a view to its underlying base table.** `/accounts/outstanding/page.tsx` (Bill-wise Outstanding, built in Phase 7) queried `sales_invoice_outstanding` and `purchase_invoice_outstanding` with a nested embed (`select("...,sales_invoices(invoice_date, due_date, customers(name))")`) - this has been silently returning a `PGRST200` error ("Could not find a relationship") since the page was first built, which the page's `data ?? []` fallback turned into an always-empty "Nothing outstanding" result with no visible crash. Confirmed via a control test that the completely untouched `purchase_invoice_outstanding` embed fails identically, proving this predates Phase 9 entirely. Fixed in `20260817800004_fix_outstanding_view_embeds.sql` by denormalizing `customer_name`/`invoice_date`/`due_date` (and `supplier_name`/`supplier_invoice_date`/`due_date`) directly onto the two views (required `drop view` + `create view`, not `create or replace` - Postgres won't let a positional column be inserted/renamed via replace) and rewriting the page to select flat columns instead of embedding. Confirmed via a dedicated Explore agent that this was the *only* call site in the codebase using embed-through-view syntax - `ReceiptForm`/`PaymentForm` already only selected flat columns from these views and were unaffected. Also required an explicit `notify pgrst, 'reload schema'` after the view replacement, since the automatic schema-cache refresh didn't pick it up.
- Full Playwright + direct-DB verification, re-run after both fixes: a credit sale (with staff+route) and a cash sale (same staff) created, a receipt for part of the credit sale dated ~20 days out to land in the 16-30 day bucket. Staff Performance showed total sales 1770 (1180+590), outstanding 680 (not 1270), profit 600, and the 500 correctly bucketed under 16-30 days. Collection Report showed the same receipt with the right days-taken and status. Route Performance's totals matched, and its staff breakdown showed the right pairing. Bill-wise Outstanding now actually lists the credit invoice (previously always empty) and correctly excludes the cash sale. Trial Balance still balanced after the journal trigger fix.
- Test data cleanup followed the Gotcha #9 process throughout, including a stray duplicate customer/route left by an earlier failed test run (caught by querying first, not assumed).

**Fast-follow / not done:** None specific to this phase - it's pure reporting on already-complete data.

## Phase 10 (Incentive Engine) - COMPLETE

**Confirmed against the actual spec text** (sections 32-34, same transcript-extraction approach as Phases 8-9): a configurable incentive engine considering both Sales and Credit Collection ("do NOT calculate incentive only from sales" - section 32), configurable collection-based incentive slabs keyed to actual days-between-invoice-and-collection with an explicit example rate table that "must NOT be hard-coded" (section 33), and an Incentive Dashboard with Sales/Collection/Total incentive, Target, Achievement, Collection percentage, Average collection days, Outstanding generated/collected, and 90+ day outstanding, per staff, with date filtering (section 34).

**What shipped:**
- Migrations `20260817900001` - `20260817900005`: `company_settings.sales_incentive_rate` (a single configurable flat rate - the spec only gives a slab structure for collection, not sales, so a flat rate is the honest reading rather than inventing an unspecified sales-tier system), `collection_incentive_slabs` (8 rows seeded with the spec's own example: Ready Cash 2.00% down to 90+ Days 0%, rate editable by Admin, day-boundaries fixed in the UI to avoid gaps/overlaps), `staff_sales_targets` (per-staff-per-month target, new concept needed for "Target"/"Achievement"), `get_incentive_dashboard(from,to)` (one row per staff, same correlated-subquery architecture as Phase 9's `get_staff_performance`), and a same-day fix migration for an ambiguous-column bug (see below).
- Nav: three new sub-links under the existing `staff` module - Incentive Dashboard, Incentive Slabs, Sales Targets. `sales_incentive_rate` added to the existing Settings > Company form next to the other rate/prefix fields.
- Built: `staff/incentive-dashboard/page.tsx` (Today/This Week/This Month + custom range, same pattern as Phase 9), `staff/incentive-slabs/page.tsx` (inline-editable rate per slab via a small client component + server action, day-range shown read-only), `staff/sales-targets/page.tsx` (list + a small form that upserts on `staff_id, period_month, period_year`).
- **Caught and fixed one bug during this phase's own verification**: `get_incentive_dashboard` failed with Postgres error 42702 "column reference staff_id is ambiguous" - the function's own `RETURNS TABLE (staff_id uuid, ...)` column becomes an implicit PL/pgSQL identifier inside the function body, colliding with unqualified `staff_id` references in the `targets` CTE and the final `UNION`-based `WHERE up.id IN (...)` clause. Fixed in `20260817900005` by qualifying every reference with its table/alias. Same lesson as Phase 7/9's other bugs: `npm run build`/TypeScript can't catch a PL/pgSQL naming collision - only actually calling the function does, so the direct-RPC sanity check before wiring up the UI (a habit from Phase 9 onward) caught this before any UI code was even written against it.
- Full Playwright + direct-DB verification: set a 5% sales incentive rate and a 10,000 monthly target for Joel Varghese, created a credit sale (1180 total) and a same-day full receipt against it. Confirmed directly against the database (not just the UI): sales incentive 59.00 (1180 x 5%), collection incentive 23.60 (1180 x the Ready Cash slab's 2.00% - correctly picked the 0-day slab), total incentive 82.60, achievement 11.8% (1180/10000), collection percentage 100% (fully collected same day), average collection days 0, outstanding generated/collected both 1180.00, 90+ day outstanding 0.
- Test data cleanup followed the Gotcha #9 process; also reset `company_settings.sales_incentive_rate` back to 0 afterward since it's a real (if currently unset) business setting, not disposable test data, unlike the throwaway customer/product/invoice rows.

**Fast-follow / not done:** No tiered/achievement-based sales incentive (flat rate only, matching what the spec actually specifies for sales vs. the explicit slab structure it only gives for collection).

## Phase 11 (Dashboard + Analytics) - COMPLETE

**Confirmed against the actual spec text** (sections 40-41): a professional management dashboard with 12 "today"/current figures (Today's Sales/Purchase/Collection/Expenses, Cash/Bank Balance, Customer/Supplier Outstanding, Stock Value, Low Stock, Gross Profit, Profit Percentage, Staff Sales, Staff Collection), plus four Monday-to-Sunday weekly graphs (Sales/Collection/Purchase/Profit) with a previous-week comparison. The `/dashboard` page had been an explicit "Coming soon" placeholder since Phase 1 with a comment pointing at these exact two sections.

**What shipped:**
- Two migrations, `20260818000001`/`20260818000002` - pure reporting, no new tables: `get_dashboard_summary(p_date)` (single-row aggregate - today's flow figures filtered to `p_date`, point-in-time figures like Cash/Bank balance, outstanding, and stock value always current since the UI never exposes a past-date picker for the dashboard), `get_weekly_metrics(p_week_start)` (7 rows, Monday-Sunday, called twice from the page for this-week/last-week comparison).
- **No new charting dependency** - `src/components/weekly-bar-chart.tsx` is a small hand-rolled SVG grouped-bar-chart component (two series, day labels, legend, hover tooltips via `<title>`), avoiding any React-19-compatibility risk from a third-party charting library for what's a simple enough visual.
- Rewrote `dashboard/page.tsx` entirely: a 12-card metric grid (every card links through to its real source report - Today's Sales to Credit Sales, Customer Outstanding to Bill-wise Outstanding, Stock Value to Stock Report, etc.), two compact staff leaderboards (reusing Phase 9's `get_staff_performance(today,today)` directly rather than new SQL, top 5 by sales/collection), and the four weekly graphs in a responsive grid. Profit figures (Gross Profit, Profit Percentage, Weekly Profit graph) gated by the same `PROFIT_VISIBLE_ROLES` array used everywhere else profit appears.
- Applied the Phase 10 lesson from the start: sanity-called both new RPCs directly via REST immediately after pushing the migrations, before writing any UI code against them - both worked correctly on the first try, no repeat of Phase 10's ambiguous-column bug.
- Full Playwright + direct-DB verification: created a purchase, a credit sale, a cash sale, a full receipt against the credit sale, and an expense payment, all dated today. Every dashboard figure cross-checked directly against `get_dashboard_summary` in the database (not just the UI): today's sales 1770.00 (1180 credit + 590 cash), today's purchase 1416.00, today's collection 1180.00, today's expenses 250.00, customer outstanding 0.00 (fully paid), supplier outstanding 1416.00 (unpaid), gross profit 600.00, cost total 900.00, stock value 40300.00 (40000 pre-existing + 300 for 5 remaining units). Cash balance came out to 1520.00, not the naively-expected 930.00 (1180 receipt - 250 expense) - correctly reflects Phase 9's cash-sale fix, since a cash sale posts directly to the Cash account (590) independent of any receipt, so 590 + 1180 - 250 = 1520 is actually right. All four weekly graphs rendered with no console errors.
- Test data cleanup followed the Gotcha #9 process throughout.

**Fast-follow / not done:** None specific to this phase.

## Phase 12 (Mobile Application) - COMPLETE

**Confirmed against the actual spec text** (sections 42-45; section 46 "Offline/Poor Network Handling" is confirmed Phase 13's territory, matching the phase list's own "Realtime Sync + Offline Queue" naming, not this one). Traced the actual RBAC seed (`20260817000001_rbac.sql`) before planning anything: Sales Staff has `dashboard`/`sales`/`accounts` (view+create) and `masters` (view), but never `staff` - meaning Sales Staff accounts were completely locked out of Collection Report and Staff Performance, directly contradicting section 42's "View collection history" and "View own performance." That was the one real, concrete gap - everything else in section 42 (Sales Orders, last-selling-price lookup, Cash/Bank Receipts with bill-wise/on-account) was already fully built in earlier phases and re-verified field-by-field against sections 43-45 without needing changes.

**What shipped:**
- **Zero new migrations.** A "My Workspace" self-service area (`my/dashboard`, `my/customers`, `my/collections`) that reuses Phase 9's `get_staff_performance`/`get_collection_detail` RPCs plus plain queries against `customers`/`routes`/`sales_invoice_outstanding`, gated only by being logged in (no `can()` module check) rather than the `staff` permission Sales Staff doesn't have - every underlying table's RLS already permits it via Sales Staff's existing `sales`/`accounts`/`masters` view grants, confirmed by tracing the actual policies before writing any code. Results are filtered to the current user's own id/name **server-side**, before the page is ever sent to the browser, so no other staff member's data is exposed regardless of what the client can see.
- Nav: an always-visible "My Workspace" link added right after Dashboard for every logged-in user (not gated by a module, same treatment as Dashboard itself).
- **Found and fixed a real, severe mobile-usability bug while taking the very first mobile-viewport screenshot**: the sidebar has been a fixed `w-60` (240px) block with no responsive behavior since Phase 1 - on a 390px phone viewport it consumed most of the screen, squeezing all page content into an unusable ~150px sliver. This had been present for all 12 phases and every prior mobile screenshot (Phase 6 dark-mode shots, etc.) was actually taken at a desktop viewport, so it was never caught until this phase's first genuine narrow-viewport check. Fixed with a new `src/components/sidebar-shell.tsx` client component: sidebar is `fixed` + `-translate-x-full` (off-canvas) by default below the `lg:` breakpoint, toggled by a hamburger button in a new mobile top bar, with a tap-to-dismiss backdrop and auto-close on nav-link click; unchanged and always-visible at `lg:` and above. No changes to the `<aside>` content itself, `layout.tsx`'s data-fetching, or any permission logic - purely a presentational wrapper.
- Full Playwright verification at both desktop (1280px) and mobile (390px) viewports: confirmed the sidebar is genuinely off-screen on mobile via screenshot (not just an `isVisible()` check, which doesn't distinguish "transformed off-canvas" from "visible" - see Gotcha #14), confirmed the hamburger menu opens/closes and closing-on-navigation works, confirmed desktop is pixel-identical to before. Created a route and a customer both assigned to a staff member, a credit sale, and a receipt; confirmed `/my/dashboard` showed the correct today's-sales/route/customer-count figures, `/my/customers` showed the right outstanding balance, `/my/collections` showed the receipt with the right days-taken. Confirmed the PWA manifest still serves correctly with a valid icon set.
- **One remaining rough edge, disclosed rather than silently left**: `LineItemRow` (the shared product-line component used by Sales, Purchase, Credit Notes, and Debit Notes) uses a fixed `grid-cols-12` layout that doesn't reflow on narrow screens - qty/rate/discount fields are still fully functional and tappable on a phone, but their placeholder text visually truncates. Not fixed this phase since it's a cross-cutting change to a component used by four different transactional flows, and the phase's own scope decision was to verify-not-rebuild those flows; flagged as a fast-follow.
- Test data cleanup followed the Gotcha #9 process.

**Fast-follow / not done:** `LineItemRow`'s responsive layout on narrow screens (functional, cosmetically cramped - see above).

## Phase 13 (Realtime Sync + Offline Queue) - COMPLETE

**Confirmed against the actual spec text** (sections 46-47, same transcript-extraction approach as every phase since 8): Offline/Poor Network Handling wants a local transaction queue for permitted transactions when internet drops - client-assigned UUID, auto-sync on reconnect, duplicate prevention, a visible Pending Sync -> Uploading -> Synced -> Confirmed status flow, and an explicit "never silently lose a transaction" mandate (46). Web + Mobile Data Sync wants a receipt entered on mobile to show up within seconds on the web dashboard's Collection figure, Customer Ledger, Outstanding, Staff Collection, and Cash/Bank balance (47). Scoped the offline queue to exactly the three flows sections 42-45 frame as mobile/field work (Sales Order, Cash Receipt, Bank Receipt) rather than every form in the app - nothing in those sections asks a field rep to pay a supplier or file a return offline.

**What shipped:**
- Migrations `20260818100001`-`20260818100003`: added `sales_invoices`, `receipts`, `receipt_allocations`, `payments`, `payment_allocations`, `journal_entries`, and (in a same-day fix, see below) `purchase_invoices` to the `supabase_realtime` publication; `create_receipt`/`create_payment` gained a trailing optional `p_client_id uuid default null` - if a row with that id already exists, the RPC returns it immediately instead of re-inserting (had to `drop function` + recreate rather than plain `create or replace`, since adding a parameter changes a Postgres function's identity/creates a new overload rather than replacing in place).
- **Idempotency reuses the primary key itself, no new column.** `sales_orders` was already a plain client-writable table with a `default gen_random_uuid()` id, so the browser just supplies its own `crypto.randomUUID()` as the `id` on insert - a retry hits the PK constraint (23505), caught and treated as "already synced." `create_receipt`/`create_payment` do the equivalent via the new `p_client_id` parameter.
- `src/lib/offline-queue.ts` - a `localStorage` queue namespaced by user id (`skye-erp-offline-queue:<userId>`), so a shared device logged in as different people doesn't cross-contaminate queues.
- `src/components/offline-sync-manager.tsx` - mounted once in `layout.tsx`, flushes pending/failed queue items on mount and on the browser's `online` event, writing directly via `supabase-js` (not the page's server actions, which can't be reused headlessly here - see below).
- **Refactored `createReceipt`/`createSalesOrder` away from `redirect()`.** Both previously called Next.js's `redirect()` on success; wrapping that call in a client-side `try/catch` to detect offline failures risked misinterpreting a successful-but-redirecting response as a network error (an unresolved, version-sensitive edge case in how Server Action redirects surface to the calling client code). Refactored both to return `{ error, id }` instead, with the form doing `router.push()` on success - removes the ambiguity entirely and is the safer, more testable pattern regardless of the offline feature.
- `receipt-form.tsx`/`sales-order-form.tsx`: generate the client UUID upfront, check `navigator.onLine` first (skip the online attempt entirely if already known-offline), fall back to `enqueue()` inside a `catch` for the residual "looked online but wasn't" case. Shows a "Saved offline" panel instead of navigating.
- `src/app/(app)/my/sync/page.tsx` (+ `sync-status-list.tsx` for the live-polling table) - this device's queue with Pending Sync/Uploading/Synced/Failed status pills, under the Phase 12 "My Workspace" area since the queue is device-local. "Synced" and "Confirmed" were deliberately collapsed into one state - a successful Postgres/PostgREST response is already a durable commit, there's no meaningful separate confirmation round-trip to fake. `src/components/sync-badge.tsx` shows a pending-count badge next to "My Workspace" in the sidebar.
- `src/components/realtime-refresh.tsx` - subscribes to `postgres_changes` on a given table list and calls `router.refresh()` (debounced 500ms) so the enclosing Server Component re-fetches. Mounted on the six pages section 47 names: Dashboard, My Workspace dashboard, Customer Ledger, Bill-wise Outstanding, Staff Performance, Collection Report.
- **Caught and fixed a real gap during verification**: `accounts/outstanding` needs `purchase_invoices` changes for the supplier side, which was missed in the initial realtime-enable migration (only sales-side tables were added) - fixed in `20260818100003`.
- Full Playwright verification: confirmed the online baseline (create a Sales Order, a Cash Receipt) still navigates correctly after the `redirect()` -> `router.push()` refactor - a real regression risk that was explicitly checked, not assumed. Loaded both forms while online, then simulated `context.setOffline(true)` and submitted - both showed "Saved offline" and queued correctly (confirmed via direct `localStorage` read, not just the UI). Brought the connection back and confirmed both items reached "Synced" and the underlying rows exist in the database with no duplicates. **A genuine race condition surfaced and validated the idempotency design itself**: navigating to `/my/sync` right after reconnecting triggered a second `OfflineSyncManager` mount whose own flush overlapped with the first, producing one console 409 (unique-violation) - but the database ended up with exactly the right rows and no duplicates, confirming the `p_client_id`/client-supplied-`id` short-circuit works correctly under real concurrent retries, not just in theory. For realtime: two separate browser contexts, one watching Bill-wise Outstanding, the other creating a new customer's credit sale - the first context picked up the new customer within ~4 seconds with no manual reload (confirmed via an explicit `SUBSCRIBED` channel-status check during debugging, then reproduced cleanly with debug logging removed).
- Test data cleanup followed the Gotcha #9 process, including a duplicate test customer left over from an earlier failed debug run (dev server didn't restart cleanly via `Start-Job` mid-session - reverted to the standard background-task pattern).

**Fast-follow / not done:** No offline support for Cash/Bank Payments (out of scope per the spec's own mobile-flow framing - see scope decision above). No offline page-shell caching (section 46 is specifically about the transaction queue, not general offline browsing - a user still needs connectivity to load a page for the first time).

## Established architecture / conventions (apply to every future phase)

- **RBAC**: `public.has_permission(module_key, action)` and `public.is_admin()` Postgres functions (Phase 1). Module keys currently seeded: dashboard, sales, purchase, inventory, accounts, gst, staff, reports, masters, settings - each with view/create/edit/delete/export actions. `src/lib/auth/permissions.ts` has the client-side `can(user, module, action)` mirror and `getCurrentUser()`.
- **Audit trail**: `public.log_audit()` trigger (Phase 1), attach to any new table's insert/update. Table `audit_logs`, viewable via `RecordHistory` component (`src/components/record-history.tsx`) filtered by table_name+record_id.
- **Money-critical writes go through RPCs, not direct table access**: any table involving invoice numbering, GST calculation, or stock movement (sales_invoices, purchase_invoices, stock_transactions) has **no insert/update policy for `authenticated`** - only a `security definer` RPC can write, so the atomic numbering/GST/stock logic can never be bypassed by a direct client call. Plain master-data tables (customers, products, sales_orders, etc.) use normal has_permission-gated CRUD policies instead.
- **Invoice numbering pattern**: atomic via `UPDATE company_settings SET next_X_number = next_X_number + 1 WHERE id = (select id from company_settings limit 1) RETURNING ... INTO ...` in one statement - the WHERE clause is mandatory (see Gotchas) and doing the increment+read as one UPDATE (not SELECT then UPDATE) is what makes it race-safe.
- **Deletion policy**: nothing gets hard-deleted. Every transactional entity has a **Cancel** action instead (reverses stock, marks status='cancelled'), matching spec section 48. Master data uses `is_active` deactivate/reactivate toggle instead of delete.
- **Stock**: single ledger table `stock_transactions` (product_id, quantity_change signed, transaction_type, reference_table, reference_id) introduced in Phase 3 ahead of the "official" Inventory phase (5) because Sales needed it to be real. Current stock for a product = `opening_qty + sum(quantity_change)`. Phase 5 should build reporting on top of this, not redefine it.
- **GST split**: CGST+SGST vs IGST determined by comparing `company_settings.state` to the counterparty's (customer or supplier) `state` field - same-state = CGST/SGST split in half, different = IGST. Company state must be set on Settings > Company for this to work correctly (it's blank by default/after test cleanup - remind the user to set their real state before real billing).
- **Dark mode**: every new component must include `dark:` Tailwind variants. Established color recipe (light -> dark): `bg-white`->`dark:bg-gray-900`, `bg-gray-50`->`dark:bg-gray-950`, `bg-gray-100`->`dark:bg-gray-800`, `border-gray-200`->`dark:border-gray-800`, `border-gray-300`->`dark:border-gray-700`, `text-gray-900`->`dark:text-gray-100`, `text-gray-700`->`dark:text-gray-300`, `text-gray-500`/`600`->`dark:text-gray-400`, `text-gray-400`->`dark:text-gray-500`, `text-blue-600`->`dark:text-blue-400`, status badges use `bg-{color}-50 dark:bg-{color}-950/40 text-{color}-700 dark:text-{color}-400`. `color-scheme: light dark` is set in `globals.css` root.
- **Forms**: every text input/textarea gets `autoComplete="off"` (password fields creating a new user get `"new-password"`) - browsers were offering irrelevant autofill suggestions on business-data fields otherwise.
- **Shared components** (`src/components/`): `SearchInput`, `Pagination`, `StatusBadge`, `DeactivateButton`, `RecordHistory`, `RouteFilter`, `LineItemRow` (+ `ProductOption`/`LineItem` types), `CancelInvoiceButton`. Reach for these before writing a new one-off.
- **List pages**: server component, `.range()` pagination (`PAGE_SIZE = 20`), `ilike` search via the shared `SearchInput` (writes `?q=` to the URL), reuse `Pagination`.
- **Create/edit forms**: dedicated `/new` page + a shared form component parameterized where the create/edit flows are near-identical (e.g. one `SalesInvoiceForm` for both credit and cash). Multi-line-item forms (invoices/orders) are client components with local array state, not raw `<form action>` FormData, because the data is structured/nested.
- **Company info**: `company_settings` is a true singleton (unique index on `(true)`), always UPDATE never INSERT from the app.
- **Journal posting (Phase 7 onward)**: every money-moving table has an `AFTER INSERT OR UPDATE` (or `AFTER INSERT` only, for immutable tables) trigger that posts a balanced double-entry journal entry to `journal_entries`/`journal_entry_lines`, using the `public.coa_id(code)` helper to look up accounts by their fixed code (see `chart_of_accounts` seed in `20260817600001`). **Any future phase that adds a new table representing money moving (a new payment type, a return, a write-off) needs a matching trigger function**, or that activity will silently never appear in Trial Balance/P&L/Balance Sheet - there is no other mechanism that populates the journal. If the source table's RPC inserts a header row before totals are finalized (check whether it does an INSERT-then-UPDATE, like `create_sales_invoice`/`create_purchase_invoice` do - see Gotcha #10), post on the totals-finalizing UPDATE, not blindly on INSERT.
- **Cash sales never touch Accounts Receivable** (fixed in Phase 9, see the Phase 9 writeup above): `post_sales_invoice_journal` debits Cash (`1000`) for `sale_type = 'cash'` and Accounts Receivable (`1100`) for `sale_type = 'credit'` - only credit sales have an outstanding-balance lifecycle. `sales_invoice_outstanding` and `get_customer_ledger` both filter to `sale_type = 'credit'` for the same reason. Any new report/RPC touching customer receivables must filter the same way, or a cash sale will silently inflate "outstanding."
- **Never embed through a view** - see Gotcha #12. Query flat/denormalized columns from a view, or join the base table separately in application code.
- **"My own data" self-service pages don't need a module permission** - see Phase 12's `/my/*` pages. If every underlying table's RLS already permits a role's existing grants (check the actual policies, don't assume), a page showing only the current user's own row can skip the `can(user, module, action)` gate entirely and just require being logged in - filter to `user.id`/`user.fullName` **server-side** before the page renders, never send the unfiltered result to the client. This is different from an admin-facing report over the same RPC, which does still need the module permission.
- **The sidebar is responsive from Phase 12 onward** - `src/components/sidebar-shell.tsx` wraps the `<aside>` in `layout.tsx`, off-canvas below the `lg:` breakpoint (hamburger toggle + backdrop), always visible at `lg:` and above. Any future change to the sidebar's own content goes in `layout.tsx` as before; only the responsive show/hide behavior lives in the shell component.

## Gotchas (hit these once already - don't repeat)

1. **Supabase's managed Postgres rejects any `UPDATE`/`DELETE` with no `WHERE` clause** - even inside a `security definer` plpgsql function, even via the REST API. Error: "UPDATE/DELETE requires a WHERE clause". Always add one (`where id = (select id from x limit 1)` for a singleton table; `?id=not.is.null` for ad-hoc REST cleanup queries).
2. **Stale Next.js dev server Server Action manifest**: after many hot-reloads across a long dev session, clicking a form's submit button can silently invoke the *wrong* server action (observed: a "Save" button on a brand-new page triggered `logout()` from an unrelated file). Symptom: form submit redirects to `/login` for no reason. Fix: fully kill and restart `npm run dev` (`Get-NetTCPConnection -LocalPort 3000` -> `Stop-Process`, then relaunch) before any serious Playwright verification pass, not just rely on Fast Refresh.
3. **FK-dependency order matters when cleaning up test data via the REST API**, and don't assume a DELETE call succeeded just because the script didn't visibly error - check each response. `sales_orders.converted_invoice_id` references `sales_invoices`, so orders must be deleted *before* invoices (backwards from the child-items-first order you'd expect). A 409 on one table silently leaves rows that then block deletes on what looks like an unrelated table later in the script. Always re-query counts after a cleanup pass to confirm zero, don't trust the script's own success message.
4. **Playwright selector ambiguity**: the sidebar's persistent "Sign out" button is also `button[type="submit"]` and appears on every page - a bare `page.click('button[type="submit"]')` on an authenticated page can hit the wrong button (DOM order puts the sidebar first). Scope selectors to `main button[type="submit"]`, or better, give form fields explicit `name` attributes and target those.
5. **Browsers apply native dark-mode styling to form controls independent of the page's own CSS** unless the page sets `color-scheme` - this is what caused the original "unreadable input text" bug reported by the user (looked like a CSS bug, was actually a missing `color-scheme` declaration plus zero `dark:` variants anywhere in the app).
6. **Supabase-js RPC arg types are stricter than the actual DB function** when a plpgsql function parameter has no SQL `DEFAULT` - generated `Args` types mark nullable-in-practice params as required non-null strings. Cast the call's argument object to `Database["public"]["Functions"]["fn_name"]["Args"]` (or `as unknown as ...` when a literal `null` is involved and TS complains about "insufficient overlap").
7. **create-next-app scaffolds a `.claude/` dir with local session settings** - gitignored, not committed. Also Next.js 16 deprecated the `middleware.ts` convention in favor of `proxy.ts` (rename the file, rename the exported function from `middleware` to `proxy`, functionally identical) - already done, just don't reintroduce a `middleware.ts`.
8. **Not all data in the dev database is test data I created** - the user has their own login and has been exploring the live dev server directly in their browser (asked for the admin password more than once, reported a UI bug via screenshot). Before any test-data cleanup pass, check `created_at` timestamps and cross-reference against what your own scripts actually created - don't assume everything matching a loose pattern is yours to delete. When in doubt, leave it.
9. **CRITICAL, learned the hard way in Phase 5**: cleanup deletes on *transactional* tables (`sales_invoices`, `purchase_invoices`, their `_items`, `stock_transactions`, etc.) were written as blanket `?id=not.is.null` deletes - correct in Phase 3/4 because those tables were verified empty before the test run started, so "everything in the table" and "everything my script made" were the same set. In Phase 5 that assumption silently broke: the user had their own real sales invoice (INV00001, for the "Test"/123 product) sitting in `sales_invoices`, and the blanket delete wiped it along with the test rows. **Never use an unfiltered delete on a transactional table again.** Before any cleanup pass: (a) query the table's count/contents *first* and note which rows predate your test run, (b) delete only by the specific IDs your script actually created (capture them as you go), never by "delete everything in this table." This applies even when a table looked empty last time you checked - re-verify every time, don't rely on memory of a prior phase's state.
10. **A trigger posting on `AFTER INSERT` can still see zero/default values** if the row's RPC inserts a header first and fills in real totals with a later UPDATE (both `create_sales_invoice` and `create_purchase_invoice` do exactly this - insert the invoice row, loop over line items, then one final `UPDATE ... SET subtotal = ..., total_amount = ...`). A trigger written to post on INSERT alone will fire at the wrong moment and post a structurally-correct but zero-amount entry - and a naive "does the Trial Balance say Balanced" check won't catch it, because 0 = 0 is still balanced. Caught in Phase 7 only by cross-checking `journal_entry_lines` amounts directly against the database, not just trusting a UI summary message. Fix: post on the UPDATE where `OLD.total_amount = 0 AND NEW.total_amount <> 0` (in addition to, or instead of, INSERT) if the source RPC follows this insert-then-finalize pattern - check the actual RPC body before assuming INSERT is the right trigger point.
11. **The environment's own auto-mode safety classifier can block a mutating action mid-verification** - even a same-session, test-data-only "cancel this invoice" call (both a direct RPC call and a Playwright-driven UI click were blocked in Phase 7) if it pattern-matches as an unreviewed write against what looks like a production database. Don't fight it or try to route around it through another tool - fall back to code review of the mirrored logic and say so honestly in the log rather than claiming a live test that didn't happen.
12. **PostgREST cannot auto-detect an embeddable foreign-key relationship from a database VIEW to its underlying base table** - `.from("some_view").select("*, some_base_table(col)")` fails with `PGRST200 "Could not find a relationship"` even though the view's own column is genuinely sourced from that table's FK. It only works from a real table to another real table (an actual FK constraint PostgREST can introspect). This bit `sales_invoice_outstanding`/`purchase_invoice_outstanding` (both Phase 6 views) silently - the failed query's `data` came back `null`, and `(data ?? []).map(...)` swallowed it into an empty array with no visible error, so Bill-wise Outstanding just always said "Nothing outstanding" from the day it shipped in Phase 7 until caught in Phase 9. **Never embed through a view** - either denormalize the columns you need directly onto the view (what was done here), or query the base table separately and join in JS. If a view's shape ever needs to change (e.g. adding a column), remember `create or replace view` cannot insert/rename a column in the middle of the list, only append at the end - use `drop view` + `create view` instead, and follow it with `notify pgrst, 'reload schema';` since the automatic cache refresh doesn't always pick up view replacements promptly.
13. **A `plpgsql` function's `RETURNS TABLE (col_name ...)` column names become implicit variables inside the function body** - if a CTE or subquery inside the function also has a column with that same name and references it unqualified, Postgres raises `42702 "column reference is ambiguous"` at call time, not at migration-push time (the `CREATE FUNCTION` itself succeeds even though the body is broken - this is a runtime error, not caught by `npm run build`/TypeScript either, since the client only sees the RPC's declared signature). Hit in Phase 10's `get_incentive_dashboard`, whose `RETURNS TABLE (staff_id uuid, ...)` collided with several CTEs' own `staff_id` columns referenced unqualified in a `UNION`-based `WHERE ... IN (...)`. Always qualify every column reference inside a `plpgsql` function body with its table name or alias, especially anything that also appears in the `RETURNS TABLE` list - and do a direct RPC sanity call (`curl`/REST, not just the UI) right after pushing any new `plpgsql` function, before writing UI code against it.
14. **Every mobile-viewport check before Phase 12 was actually taken at a desktop viewport** - Playwright screenshots throughout this project (dark-mode passes, help-button checks, etc.) never explicitly set a narrow `viewportSize`, so they defaulted to Playwright's standard ~1280px width. This meant the sidebar's complete lack of responsive behavior (a fixed 240px block, present since Phase 1) went undetected for 11 phases despite the app being built specifically as a mobile-first PWA - it was only caught in Phase 12 by deliberately setting `page.setViewportSize({ width: 390, height: 844 })` before screenshotting. **Any phase that touches or claims to verify a mobile/responsive experience must explicitly set a narrow viewport for at least one Playwright pass** - the default viewport will silently hide layout bugs that only manifest on an actual phone-sized screen. Relatedly, Playwright's `locator.isVisible()` does not distinguish a `transform: translateX(-100%)`-hidden element from a genuinely visible one (it still has a non-zero bounding box) - to verify an off-canvas/slide-in element is actually off-screen, check a screenshot or the element's bounding box position, not `isVisible()`.

## Tokens / access (for reference, already used this session - handle carefully, don't display back to the user again)

- Supabase access token, GitHub auth (via `gh` CLI device flow, already authenticated as `skyeautomotives` on this machine), and the DB password are stashed in the session scratchpad directory, not in this repo. If a fresh session needs them and they're not in the scratchpad anymore, ask the user rather than guessing.
- Migrations are pushed via `npx supabase db push --linked --password <pw> --yes` with `SUPABASE_ACCESS_TOKEN` env var set inline per-command (PowerShell doesn't persist env vars across tool calls).

## Notes for whoever (or whatever session) picks this up

- Read this file first, then `git log --oneline` for the real commit history, then the current plan file if one exists.
- Follow the phase order in spec section 57 unless the user redirects.
- Keep `docs/USER_GUIDE.md` updated at the end of every phase (user explicitly asked for this, separate from this log).
- No em dashes anywhere (chat, code, commits, docs) - standing user rule, use a plain hyphen.
- User is a beginner to app development - keep explanations plain, don't assume CLI/tooling familiarity in chat responses (this log itself can be as technical as needed, it's not user-facing).
